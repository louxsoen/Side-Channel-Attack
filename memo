12 X > 이거 켜져 있었어 
CHIP~ x

CHIP1~ O  -> 수집이 안 되는디 
: 나연이 누나꺼 가져온거 같은데

CHIP 복제 ~ X
: localhost연결이 안 됨 
> jupyter notebook 쳐라 
> 비번 cwj9731 ( 방학때 예찬이형이 알려주신 비밀번호 )


가중합 부분만 100만 포인트 

하드코딩이랑 반복문 쓴거 파형에 차이 있는지 확인 

트리거랑 원래 파형이랑 잘 조절해야지 파형이 잡힌다 > 트리거만 조절해서 안 됨



samples : 24435 
> 이거랑 상관 없나?
ERROR:root:Received fewer points than expected! 24438 vs 1000000

################
tanh 함수   00
sigmoid 함수   01

[1] 출력노드 함수 바꾸기 

[2] 
같은 가중치에 대해서 
입력 랜덤화하기 
> 여러개 파형 저장해보기 

[3] 가중치 + 입력 랜덤화 



칩위스퍼에 파일이 생기는거 같기는해 
오실로스코프에서 어캐 넘어온거지?
칩위스퍼 설정에서 바로 저장할 수 있도록 
auto save로 됨 
트리거 깜빡거리면 안됨 캡쳐가 제대로 안 됨 
> 안 잡히는거는 알아서 걸러주지 않을까?
> 근데 순서대로 다 저장되는거 같은데
> 제대로된것만 순서대로 저장하는게 아닐까?

파형 행렬로 싹 저장하도록 

트리거가 제대로 잡히는 파형은 특정 지점에서 3.37이상의 피크가 뜸 
> 이 피크가 존재하는 파형만이 옳은 파형이라고 생각 
> X축 값 지정해줘야함 
> 파형 수집할 때마다 달라질 수 있음 
> 예외가 있어서 힘들어 
> corrcoef 값으로 판단해도됨
> 옳은 파형끼리는 높게 나와 
> 옳은 파형 1개 기준 잡아서 나머지 쭉 값 비교해서 높은 것만 파형으로 저장시켜라 
> 맨 앞부분 범위 정해서 corrceef 하면 될거 같다 
> 가중치 연산 부분 

corrcoef 하면 파형 전체 부분에 대한 상관계수 구할 수 있게됨 


맨 첫번째 파형을 넘기면 뒤로 쭉 읽을 수 있게 
> 있던거를 제거하고 하기는 힘드니까 C1-wide_version2만 넘기면 다 읽을 수 있도록 
> 근데 CPA를 Matlab에서 하는게 아니라 C에서 하는거잖아 
> CPA할 때는 C에서 할 수 있도록 파형을 볼 때는 Matlab에서 볼 수 있도록 해야겠네 

######################################


- 오실로스코프 
수집시간이랑 연산 넘거주는 시간이 차이나서 넘겨주는 시간 끝나면 수집 멈춤 


####################################
/usr/lib/gcc/arm-none-eabi/5.4.1/../../../arm-none-eabi/bin/ld: simpleserial-base-CW308_STM32F3.elf section `.bss' will not fit in region `RAM'
/usr/lib/gcc/arm-none-eabi/5.4.1/../../../arm-none-eabi/bin/ld: region `RAM' overflowed by 1384 bytes
> float값 3000개는 너무 많나?  절만만 해볼까 

excess elements in array initializer
> 배열 크기 부족 

74 ~ 406 줄  -> +1 = 1줄 = 입력 3개  -> +200 -> 입력 600개 

406 - 74 + 1 = 333  

파형은 나눠서 구하면 되니까 


다시 연결하려고 하니까 추가할 수 없다고 나옴 그리고 칩위스퍼 꺼짐 



Collect_Trace 에서 무슨 설정을 바꾼건 아니고 그냥 Base에 있는 모든 걸 컴파일 하네 simpleserial-base.c를 main.c로 바꾼거 같음 
> makefile 필요하네 그냥 안 됨 


- 칩위스퍼 파일 분리 
파형 모을 때 헤더 파일 선택해서 컴파일 가능?
간단한 함수는 걍 .c에 넣으면 안 되나? .h도 만들어야해?
앞에 선언들 다 분리 시키기 


오실로스코프용 simpleserial-base.c만들기 


배열도 전역에 선언해야하나?

입력층의 값을 설정하고 활성함수 입력을 생각해야하니까 복잡하네 


랜덤한 3개의 값을 더해서 -5부터 차례로 나오도록 해야해 

실수값은 원치 않게 소수점 하위에 값이 붙여서 나옴 > 정확한 숫자가 나오는게 아니라서 오차가 많이 발생할거 같다 > 헤밍웨이트에 영향 많이 미칠까?

이 input 배열을 이용해서 활성함수의 입력을 알아 낼 수 있다 



- 할일
칩위스퍼 연산 넘겨주는 시간 조절 
입력값 제대로 들어가는 확인 





